server.port=8888
spring.application.name=config-server

# Eureka (so config server registers itself)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

autoconfigure.exclude=org.springframework.cloud.config.client.ConfigServiceBootstrapConfiguration

# Local config repo used by the config server (repo in workspace)
spring.cloud.config.server.git.uri=file://${project.basedir}/../config-repo
spring.cloud.config.server.git.search-paths=.
spring.cloud.config.server.git.clone-on-start=true
spring.cloud.config.server.git.default-label=main




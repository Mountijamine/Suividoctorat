package com.devbuild.gestionauth.archived;

/**
 * Archived: WebController moved out of active controllers to disable server-side Thymeleaf flows.
 * Keep this file in the archived package for reference and possible future migration.
 */
public class WebController {
    // Archived copy of WebController is intentionally left empty.
}

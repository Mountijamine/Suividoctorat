package com.devbuild.gestionauth.controller;

// DoctorantController intentionally disabled in gestion-auth-service.
// Doctorant-related APIs belong to the inscription-service (domain module).
// This placeholder preserves file history and avoids accidental endpoint exposure.
public class DoctorantController {
    // intentionally empty
}

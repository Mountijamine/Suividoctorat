package com.devbuild.gestionauth.controller;

// LoginController ping endpoint removed — health/ping should be provided by gateway or a dedicated actuator endpoint.
public class LoginController {
    // intentionally empty
}
